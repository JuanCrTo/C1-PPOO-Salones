@model Salones.Models.EquipoTecnologico

@{
    ViewData["Title"] = "Detalles";
}

<h1 class="text-light text-center">Detalles del Equipo Tecnológico</h1>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="bg-dark p-4 rounded shadow-sm">
                <h4 class="text-light">Equipo Tecnológico</h4>
                <hr class="border-light" />
                <dl class="row text-light">
                    <dt class="col-sm-3">
                        @Html.DisplayNameFor(model => model.Nombre)
                    </dt>
                    <dd class="col-sm-9">
                        @Html.DisplayFor(model => model.Nombre)
                    </dd>
                    <dt class="col-sm-3">
                        @Html.DisplayNameFor(model => model.Salon)
                    </dt>
                    <dd class="col-sm-9">
                        @Html.DisplayFor(model => model.Salon.Id)
                    </dd>
                </dl>

                <div class="text-center">
                    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-primary w-100 mb-2">Editar</a>
                    <a asp-action="Index" class="btn btn-secondary w-100">Volver a la Lista</a>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
